#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
<<<<<<< HEAD
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
=======
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
>>>>>>> main
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../babel-plugin-react-compiler/node_modules/jsesc/bin/jsesc" "$@"
  ret=$?
else 
  node  "$basedir/../babel-plugin-react-compiler/node_modules/jsesc/bin/jsesc" "$@"
  ret=$?
fi
exit $ret
